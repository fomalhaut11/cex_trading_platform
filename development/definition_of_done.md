# Definition of Done

A task is complete when:

- its public contract and ownership are documented;
- implementation contains type annotations and no venue leakage;
- happy path, invariants and relevant failure paths are tested;
- tests pass without network access;
- public exports and schema versions are updated;
- project progress and affected ADRs are current;
- no secret, blocking hot-path I/O or unbounded queue was introduced.

